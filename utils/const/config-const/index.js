const senderMail = process.env.senderMail;
const secretKey = process.env.secretKey;
const appSpecificPass = process.env.appSpecificPass

module.exports = {
  senderMail,
  secretKey,
  appSpecificPass
};
